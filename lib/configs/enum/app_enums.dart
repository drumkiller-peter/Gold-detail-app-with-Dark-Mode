enum MessageType {
  error,
  success,
  warning,
}

enum AuthenticationStatus {
  unknown,
  authenticated,
  unAuthenticated,
}

enum AppBottomNavItemStatus {
  dashboard,
  profile,
}

enum AppThemeMode {
  darkMode,
  lightMode,
}
